# 000 BASH FILE SETUP

The very first thing we need to do when setting up a bash file is to include at the top of the page the fabulous shebang:

```bash
#!/bin/bash
```

From there, we need to give the file proper permissions to make it executable:

```bash
# chmod(change mode) +(add) x(executable) to script.sh
chmod +x script.sh
```